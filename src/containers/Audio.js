import React from "react"; 
import PropTypes from "prop-types";
/* ----- COMPONENT IMPORTS ----- */
/* ----- GRAPHQL IMPORTS ----- */
//import UPDATE_AUDIO from "../graphql/UPDATE_AUDIO";

/*---undefined---*/

const Audio = ({}) => {
	
	return(
		<div>Audio
		</div>
	)
}

Audio.propTypes = {
}

export default Audio;