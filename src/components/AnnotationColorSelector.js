import React from "react"; 
import PropTypes from "prop-types";
/* ----- COMPONENT IMPORTS ----- */

/*---Render color selector inside annotation modal---*/

const AnnotationColorSelector = ({updateHighlight}) => {
	
	return(
		<div>AnnotationColorSelector
		</div>
	)
}

AnnotationColorSelector.propTypes = {
	updateHighlight: PropTypes.func
}

export default AnnotationColorSelector;