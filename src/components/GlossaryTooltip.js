import React from "react"; 
import PropTypes from "prop-types";
/* ----- COMPONENT IMPORTS ----- */
/* ----- GRAPHQL IMPORTS ----- */
//import OPEN_ANNOTAION_MODAL from "../graphql/OPEN_ANNOTAION_MODAL";
//import ADD_HIGHLIGHT from "../graphql/ADD_HIGHLIGHT";
//import UPDATE_HIGHLIGHT from "../graphql/UPDATE_HIGHLIGHT";

/*---Opens on click from a glossary term in Book---*/

const GlossaryTooltip = ({selection}) => {
	
	return(
		<div>GlossaryTooltip
		</div>
	)
}

GlossaryTooltip.propTypes = {
	selection: PropTypes.object
}

export default GlossaryTooltip;